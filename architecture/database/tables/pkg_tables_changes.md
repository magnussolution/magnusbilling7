# pkg_tables_changes

| Column | Definition |
|---|---|
| `id` | int(11) NOT NULL AUTO_INCREMENT |
| `module` | varchar(300) NOT NULL |
| `last_time` | varchar(18) NOT NULL |
| PRIMARY | KEY (`id`) |
